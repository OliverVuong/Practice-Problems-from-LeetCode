import org.junit.Test;
import org.junit.Before;
import org.junit.*;
import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertFalse;
import static org.junit.Assert.assertTrue;
import static org.junit.Assert.assertThat;

public class Test62_UniquePaths{
/*
    @Test
    public void testFactorial() {
        assertEquals(A62_UniquePaths.factorial(0), 1);
        assertEquals(A62_UniquePaths.factorial(1), 1);
        assertEquals(A62_UniquePaths.factorial(2), 2);
        assertEquals(A62_UniquePaths.factorial(3), 6);
        assertEquals(A62_UniquePaths.factorial(6), 720);
    }

    @Test
    public void testUniquePaths() {
        assertEquals(A62_UniquePaths.uniquePaths(3, 2), 10);
        assertEquals(A62_UniquePaths.uniquePaths(7, 3), 28);
        //assertEquals(A62_UniquePaths.uniquePaths(36, 7), 28);
    }
*/
}